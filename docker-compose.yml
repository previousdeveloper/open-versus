    version: '2'
    services:
      web:
        build: .
        ports:
         - "9999:9999"
        volumes:
         - .:/code
		networks
		 - front-tier:
		 - back-tier:
      redis:
        image: redis:alpine
		container_name: redis
		ports: ["6379"]
		networks:
		 - back-tier
	
	networks:
	 front-tier:
     back-tier:
